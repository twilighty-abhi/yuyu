name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx prisma validate
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run build
        env:
          DATABASE_URL: postgresql://build:build@localhost:5432/yuyu
          AUTH_SECRET: ci-placeholder-not-used-at-build
      - run: npm audit --omit=dev --audit-level=high

  migration-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: yuyu
          POSTGRES_PASSWORD: yuyu
          POSTGRES_DB: yuyu
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U yuyu -d yuyu"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run db:deploy
        env:
          DATABASE_URL: postgresql://yuyu:yuyu@localhost:5432/yuyu
